import { MarvellouspipePipe } from './marvellouspipe.pipe';

describe('MarvellouspipePipe', () => {
  it('create an instance', () => {
    const pipe = new MarvellouspipePipe();
    expect(pipe).toBeTruthy();
  });
});
